from building import *
import os

cwd = GetCurrentDir()

src = Glob("./src/*.cpp")

include_paths = [
    cwd,
    os.path.join(cwd, "inc"),
    os.path.join(cwd, "../imu/inc"),
    os.path.join(cwd, "../log/inc"),
    os.path.join(cwd, "../workqueueManage/inc"),  # workqueueManage.h 所在目录
    os.path.join(
        cwd, "..", "..", "..", "..", "L1_middleWare", "06_bf_lib", "filter_imu"
    ),  # dyn_notch_filter.h 所在目录
    os.path.join(
        cwd, "..", "..", "..", "..", "L1_middleWare", "06_bf_lib", "filter_imu", "inc"
    ),
    os.path.join(cwd, "../param/inc"),  # param.h (getParam function)
    os.path.join(
        cwd, "..", "..", "..", "..", "L1_middleWare", "01_module", "03_debugPin"
    ),  # debugPin.h 所在目录
]

group = DefineGroup(
    "PROJECT_BF_GYRO_FILTER_EN",
    src,
    depend=[
        "PROJECT_BF_GYRO_FILTER_EN",
        "PROJECT_BF_PARAM_EN",  # getParam function
        "PROJECT_MINIFLY_TASK05_PARAM_FLYER_BF_DEFAULT_EN",
        "L1_MIDDLEWARE_06_BF_LIB_FILTER_IMU_EN",
        "PROJECT_BF_MLOG_GYRO_EN",
    ],
    CPPPATH=include_paths,
)

Return("group")
