import os
from building import *

cwd  = GetCurrentDir()
path =  [cwd]
src  = []

if GetDepend(['BSP_USING_ON_CHIP_FLASH']):
    src += Glob('fal_flash_at32f4_port.c')

group = DefineGroup('Drivers', src, depend = ['BSP_USING_ON_CHIP_FLASH'], CPPPATH = path)

Return('group')
